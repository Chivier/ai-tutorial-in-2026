# AI Tutorial in 2026

零基础普通人的 AI 使用教程，使用 mdBook 编写并通过 GitHub Actions 发布到 GitHub Pages。

## Local Preview

```bash
mdbook serve --open
```

## Build

```bash
mdbook build
```

The generated site is written to `book/` and is not committed.

